Flask==3.0.0
spacy>=3.8.0,<4.0.0
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
groq>=0.4.0
python-dotenv==1.0.0
gunicorn==21.2.0
Flask-Cors==4.0.0
cryptography>=41.0.0

# Hybrid OCR Solution (Option B - Lightweight)
opencv-python-headless>=4.8.0  # Image processing and text region detection (headless = no GUI, smaller)
numpy>=1.24.0                  # Numerical operations for cv2
Pillow>=10.0.0                 # Image operations
PyPDF2>=3.0.0                  # PDF text extraction
psutil>=5.9.0                  # Memory monitoring

# Note: easyocr removed - it pulls in PyTorch (~900MB) which exceeds Render's 512MB free tier.
# The HybridOCRExtractor falls back gracefully to pattern-based + OpenCV extraction.
# Note: No Tesseract or GCP dependencies needed!
# Memory footprint: ~150-200MB (fits Render 512MB free tier)

